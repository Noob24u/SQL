name: 'zendesk_support_analytics'
version: '1.0.0'
config-version: 2

profile: 'zendesk_support_analytics'

model-paths: ["models"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  zendesk_support_analytics:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    utils:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: core

seeds:
  zendesk_support_analytics:
    +schema: seeds
